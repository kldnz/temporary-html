apiVersion: v1
kind: Secret
metadata:
  name: webapp-secret
  namespace: webpage-upload
type: Opaque
stringData:
  # Database connection string - must match postgres-secret credentials
  # Format: postgresql://user:password@host:port/database
  DATABASE_URL: "postgresql://webapp:changeme-secure-password-here@postgres:5432/webpage_upload"

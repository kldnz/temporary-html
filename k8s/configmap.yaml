apiVersion: v1
kind: ConfigMap
metadata:
  name: webapp-config
  namespace: webpage-upload
data:
  # Base URL for generated links - CHANGE THIS to your domain
  BASE_URL: "https://upload.example.com"

  # Maximum upload size in bytes (5MB default)
  MAX_UPLOAD_SIZE: "5242880"
